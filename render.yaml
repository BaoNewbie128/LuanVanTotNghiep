services:
  - type: web
    name: jdm-car-recognition
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    healthCheckPath: /health
    envVars:
      - key: AI_MAX_UPLOAD_MB
        value: "8"
